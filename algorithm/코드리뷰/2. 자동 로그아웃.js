// setTimeout을 이용하여 2초 뒤 "자동 로그아웃"이라는 문구를 띄우는 함수를 만들고 실행해보세요.

// **`입력 인자`**

// - X

// **`주의 사항`**

// - X

setTimeout(function () {
  console.log("자동 로그아웃");
}, 2000);

setTimeout(() => {
  console.log("자동 로그아웃)");
}, 2000);
