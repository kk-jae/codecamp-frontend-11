import styled from "@emotion/styled";

const Wrapper = styled.div`
  width: 100%;
  background-color: tomato;
`;

export default function LayoutNavigation(props): JSX.Element {
  return <Wrapper>여기는 네비게이션입니다.</Wrapper>;
}
